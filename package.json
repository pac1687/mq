{
  "name": "curling",
  "version": "1.0.0",
  "description": "This node script makes calls to IBM's Natural Language Understanding API and returns the aggregate scores and categories from the URLs analyzed.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin Pacubas",
  "license": "MIT",
  "dependencies": {
    "async": "^2.6.0",
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5"
  }
}
